spring:
  datasource:
    url: jdbc:h2:mem:cubectl_identity;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate

cubectl:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
